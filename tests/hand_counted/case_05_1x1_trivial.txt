# The smallest possible grid: a single cell, no interior loop at all.
# Neither the bottom-row nor left-column attach loops run (m=1, n=1 means
# both ranges are empty), and the main double loop never runs either.
# Every operation count should be exactly 0 for every BBMS algorithm.

algorithm BBMSDppStepwise
p 1
0 0
q 1
3 4
expect nca_regular_hops 0
expect nca_shortcut_hops 0
expect shortcuts_written 0
expect dead_paths_pruned 0
expect shortcuts_extended 0
expect dead_path_walk_steps 0
